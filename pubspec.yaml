name: mealguider
description: "A new Flutter project."
publish_to: "none"
version: 0.2.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0    # Supabase client (includes auth, database, edge functions)
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true